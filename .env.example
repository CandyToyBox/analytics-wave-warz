# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=your-anon-key-here

# Supabase Service Role Key (Backend API Only)
# 🔒 CRITICAL: This key has admin privileges - NEVER expose to frontend/browser
# Required for: /api/webhooks/battles, /api/update-battle-volumes, /api/scan-quick-battles
# Get from: Supabase Dashboard → Settings → API → service_role key (secret)
# Must be set in Vercel environment variables for production deployment
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# API Server Configuration
PORT=3001
NODE_ENV=development

# Farcaster Frame Configuration
FRAME_BASE_URL=https://your-api-domain.com
APP_URL=https://your-app-domain.com

# Webhook Security (REQUIRED in Production, Optional in Development)
# Used to verify webhooks are coming from WaveWarz
# Generate with: openssl rand -base64 32
# In production: This MUST be set or webhooks will be rejected
# In development: Optional - allows testing without secret
WAVEWARZ_WEBHOOK_SECRET=your-webhook-secret-here

# Battle Update API Security
# 🔒 IMPORTANT: Generate a strong random key for production
# Frontend: VITE_BATTLE_UPDATE_API_KEY (exposed in browser, so use same key)
# Backend: BATTLE_UPDATE_API_KEY (server-side, kept secret)
# Generate with: openssl rand -base64 32
VITE_BATTLE_UPDATE_API_KEY=your-secret-api-key-here
BATTLE_UPDATE_API_KEY=your-secret-api-key-here
